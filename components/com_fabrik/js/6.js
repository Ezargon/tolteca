/*Fabrik.addEvent('fabrik.form.submit.start', function(form, event, button) {
	var select = document.getElementById("t_respuestas___estado");
	var indice = select.selectedIndex;
    var estado = select.options[indice].value;
    alert("guardando");
    if(estado == "5")
    {
    	if(confirm("\u00bfEst\u00E1 seguro de que ha finalizado la respuesta?\nSi pulsa ACEPTAR la respuesta ser\u00E1 enviada al solicitante y no podr\u00E1 volver a ser modificada")){
    			form.result = true;}
    	else{
    			form.result = false;}
    }
})*/